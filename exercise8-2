#include <stdio.h>
#include <time.h>


//if(a[i]<a[i+1])
int x;

int main(void){
    printf("enter a number:");
    scnaf_s("%d", &x);
    int a[x];
    srand(time(NULL));

    printf("\nnew list: \n");
    for (size_t i = 0; i < x; i++)
    {
        a[i] = rand() %100;
        printf("%d, ", a[i]);
    }
    printf("\nlist sorted: \n");
    for(int i=1;i<x;i++){
        while (a[i]!<a[i+1]){

        }
   
    
}


    if(a[i-1]<a[i])    //is the problem here?
{
        x=v[i-1];v[i-1]=v[i];v[i]=x;
        ok=0;
}


    int m = n;
    do {
        int last = 0;
        for (int i = 1; i < m; i++)
            if (v[i-1] < v[i])
            {
                int x = v[i-1]; v[i-1] = v[i]; v[i] = x;
                last = i;
            }
        m = last;
    } while ( !( m < 2 ) );


