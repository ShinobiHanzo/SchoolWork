#include <stdio.h>
#include <time.h>


//how to linear sort an array by decending order
//if(v[i]<v[i+1])
int x;

int main(void){
    printf("enter a number:");
    scnaf_s("%d", &x);
    int a[x];
    srand(time(NULL));

    for (size_t i = 0; i < x; i++)
    {
        a[i] = (rand %100)
    }
    
}

for(int i=1;i<n;i++)
    if(v[i-1]<v[i])    //is the problem here?
{
        x=v[i-1];v[i-1]=v[i];v[i]=x;
        ok=0;
}


    int m = n;
    do {
        int last = 0;
        for (int i = 1; i < m; i++)
            if (v[i-1] < v[i])
            {
                int x = v[i-1]; v[i-1] = v[i]; v[i] = x;
                last = i;
            }
        m = last;
    } while ( !( m < 2 ) );


